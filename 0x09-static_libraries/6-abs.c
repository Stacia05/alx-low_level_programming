#include "main.h"

/**
* _abs - absolute value of an integer is computed
* @n: the int to be checked
* Return: the absolute value of int
*/

int _abs(int n)

{
if (n >= 0)
{
return (n);
}
return (-n);
}
